# Ask the user for their name
name = input("What is your name? ")

# Define a function that greets the user by name
def greet_user(name):
    print(f"Hello, {name}! Welcome back.")

# Call the function with the name entered
greet_user(name)
